using SkiaSharp;
using Xunit;

namespace Medja.OpenTk.Test
{
    public class SkiaBitmapFactoryTest
    {
//        [Fact]
//        public void NeedsSkiaBugfix()
//        {
//            var fileName = "";
//            var neededSecondCall = false;
//            var targetImageInfo = SKBitmap.DecodeBounds(fileName);
//
//            // BUGFIX: for SkiaSharp 1.68.0 => when calling SKBitmap.DecodeBounds for the first time, it will not work
//            if (targetImageInfo.Width == 0 && targetImageInfo.Height == 0)
//            {
//                neededSecondCall = true;
//                targetImageInfo = SKBitmap.DecodeBounds(fileName);
//            }
//
//            Assert.True(neededSecondCall);
//            Assert.NotEqual(0, targetImageInfo.Width);
//            Assert.NotEqual(0, targetImageInfo.Height);
//        }
    }
}
